<UserControl x:Class="AllInOneMEP.UI.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:AllInOneMEP.UI.ViewModels">
    <UserControl.DataContext>
        <vm:MainViewModel/>
    </UserControl.DataContext>
    <Grid>
        <StackPanel>
            <Button Content="Run Mechanical" Command="{Binding RunMechanicalCommand}" />
            <Button Content="Run Electrical" Command="{Binding RunElectricalCommand}" />
            <Button Content="Run Documentation" Command="{Binding RunDocumentationCommand}" />
            <TextBlock Text="{Binding StatusMessage}" />
        </StackPanel>
    </Grid>
</UserControl>
